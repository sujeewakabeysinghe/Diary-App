import { SearchcontactPipe } from './searchcontact.pipe';

describe('SearchcontactPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchcontactPipe();
    expect(pipe).toBeTruthy();
  });
});
